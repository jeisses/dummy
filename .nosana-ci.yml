nosana:
  description: Run Test 2

global:
  image: node:16
  trigger:
    branch:
      - all

jobs:
  - name: install-deps and run test
    commands: 
      - echo test
    artifacts:
      - name: node_modules
        paths:
          - ./node_modules
  - name: test
    commands: 
      - ls node_modules
    resources:
      - name: node_modules
         